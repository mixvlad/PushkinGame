$main-margin = 1.1rem;

.game-nav {
  padding: $main-margin;
  margin: $main-margin (- $main-margin);
}

.container {
  height: auto;
}

.filled {
  background-color: #FFF0EB;
}

.button-link {
  text-decoration: none;
}
